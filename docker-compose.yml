services:
  backend:
    build: ./backend
    container_name: cardiovascular-backend
    restart: unless-stopped
    expose:
      - "8000"

  frontend:
    build: ./frontend
    container_name: cardiovascular-frontend
    restart: unless-stopped
    ports:
      - "4000:4000"
    depends_on:
      - backend
